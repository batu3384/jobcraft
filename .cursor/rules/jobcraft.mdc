---
description: Jobcraft workflows — bootstrap, profile, portals, slash-command routing
alwaysApply: true
---

# Jobcraft (Cursor)

This repo is a **Jobcraft** workspace (Turkey-first job search + LaTeX applications).

## Bootstrap

Missing tools? Run `./install.sh` then `source scripts/jobcraft-env.sh`.

- Yenibiriş cookie: `./scripts/setup-portal-auth.sh` — never ask the user to paste cookies in chat
- Portal health: `./scripts/doctor.sh`

## Profile source of truth

1. `profile.local.md` (gitignored) if it exists — real name, contact, experience
2. Else `.jobcraft/skills/job-application-assistant/01-*.md` and `PROFILE.md` (templates until `/setup`)

## Workflows (do not duplicate — read the canonical file)

| User says | Read and follow |
|-----------|-----------------|
| `/setup`, setup, profil | `.jobcraft/commands/setup.md` |
| `/scrape`, iş ara, job search | `.jobcraft/commands/scrape.md` → `.jobcraft/skills/job-scraper/SKILL.md` |
| `/rank`, shortlist | `.jobcraft/commands/rank.md` |
| `/apply`, başvuru, CV for job | `.jobcraft/commands/apply.md` |
| `/interview` | `.jobcraft/commands/interview.md` |
| `/outcome` | `.jobcraft/commands/outcome.md` |
| `/upskill` | `.jobcraft/skills/upskill/SKILL.md` |
| `/html-report` | `.jobcraft/commands/html-report.md` |

Portal CLIs live under `.agents/skills/*/cli/` — use each portal's `SKILL.md` for exact flags.

Full agent integration: `AGENTS.md`
